# cmdlnprint-base docker image

This image contains:

- debian stretch: Base system

- git, makem firefox-esr, node, npm & jpm: building of cmdlnprint

- Xvfb & x11vnc

It does not contains any firefox preference or entry point
